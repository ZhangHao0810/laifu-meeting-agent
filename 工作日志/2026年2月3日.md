我有一个 会议室预定的 api 接口文档，如何又快又好的封装这些api接口成 MCP Server ？ 我有一个AI Agent 对话程序希望调用他们。 

思路：
    直接将接口 转化为 Agent的 Tool 调用就行了呗！
    不行，要抽象通用能力，还是要用MCP Server，然后laifu-metting-agent 调用MCP Server。


任务拆解：
2天快速实现 一个前后端 对话 agent
    1. ✅会议室接口梳理。（拍照，外网处理）
    2. ✅MCP Server封装。
    3. ✅构造MCP Server 的 mock数据。会议室相关的。（人员相关的，后续再做） 
    4. 实现laifu-metting-agent，react实现对话式会议室预定。 
    5. 调试
    6. 周四开会演示。


agent 需要 加 Human-in-the-loop：敏感操作（如确认预定）让用户确认。